name: Deploy
on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        run: |
          cd /home/gshorti/src/info-game/
          git pull
          docker compose up -d --build
